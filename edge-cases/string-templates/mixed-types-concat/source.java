package edge.string_templates.mixed_types_concat;

public class Logger {
    public String format(int count, String label, double price) {
        return "count=" + count + " label=" + label + " price=" + price;
    }
}
