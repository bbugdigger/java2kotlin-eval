package io.github.bbugdigger.j2keval.eval

fun main(args: Array<String>) {
    println("eval module — not yet implemented (Phase 0 scaffolding only).")
    println("Phase 3 will wire up: --converted <dir> --gold <dir> --out <dir>")
    if (args.isNotEmpty()) println("Received args: ${args.joinToString(" ")}")
}
